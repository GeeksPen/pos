# دليل النشر (Deployment Guide)

هذا الدليل يوضح لك خطوة بخطوة كيفية نشر نظام الكاشير (POS) الخاص بك على خادم إنتاجي (Production Server) مثل DigitalOcean، AWS، أو Hostinger باستخدام تقنية **Docker**.

## 1. المتطلبات الأساسية
تأكد من توفر الآتي على الخادم الخاص بك (VPS):
- نظام تشغيل Linux (يُفضل Ubuntu 22.04 أو أحدث).
- تم تثبيت `Docker` و `Docker Compose` على الخادم.
- اتصال بالإنترنت وصلاحيات Root (أو Sudo).

## 2. نقل الكود إلى الخادم
قم بنسخ أو رفع الكود المصدري للمشروع إلى خادمك. يمكنك استخدام `git clone` إذا كان الكود مرفوعاً على GitHub أو GitLab، أو استخدام `scp` لرفع الملفات مباشرة.

```bash
git clone <رابط_المستودع>
cd <اسم_مجلد_المشروع>
```

## 3. إعداد البيئة (Environment Variables)
قم بنسخ ملف الإعدادات الخاص بالإنتاج وتفعيله:

```bash
cp .env.production .env
```

تأكد من فتح ملف `.env` (باستخدام محرر مثل `nano .env`) وتغيير القيم التالية لتناسب خادمك:
- `APP_URL`: قم بتغييره إلى النطاق (Domain) أو الـ IP الخاص بك.
- `APP_KEY`: لتوليد مفتاح جديد، سنقوم بتشغيل أمر لاحقاً.
- `DB_PASSWORD`: غيّر كلمة مرور قاعدة البيانات إلى كلمة مرور قوية.

## 4. تشغيل النظام عبر Docker Compose
الآن، سنقوم ببناء وتشغيل الحاويات (Containers) للنظام بالكامل (Backend, Frontend, Database, Nginx):

```bash
docker-compose up -d --build
```
*ملاحظة: هذه العملية قد تستغرق بضع دقائق في المرة الأولى لتحميل وبناء الصور.*

## 5. تجهيز قاعدة البيانات و Laravel
بعد أن تعمل الحاويات بنجاح، يجب أن نقوم بإعداد Laravel ليعمل بشكل صحيح:

1. توليد مفتاح التطبيق (App Key):
```bash
docker-compose exec app php artisan key:generate
```

2. إنشاء جداول قاعدة البيانات وإضافة البيانات الافتراضية (بما في ذلك حساب الإدارة):
```bash
docker-compose exec app php artisan migrate --seed --force
```
*ملاحظة: سيقوم هذا الأمر بإنشاء حساب مدير افتراضي:*
- *البريد الإلكتروني:* `admin@pos.com`
- *كلمة المرور:* `password123`

3. ربط ملفات التخزين (للصور والـ PDF):
```bash
docker-compose exec app php artisan storage:link
```

4. تحسين الأداء (Caching):
```bash
docker-compose exec app php artisan optimize
```

## 6. الوصول للنظام
الآن، يمكنك الدخول إلى النظام من خلال متصفحك عبر إدخال الـ IP الخاص بالخادم أو النطاق المرتبط به (على المنفذ 80).
- سيقوم Nginx بتوجيه طلبات الويب تلقائياً إلى لوحة تحكم Next.js.
- وأي طلب يبدأ بـ `/api` أو `/storage` سيتم توجيهه إلى خادم Laravel.

---

### ملاحظات إضافية
- **إيقاف النظام:** `docker-compose down`
- **عرض السجلات (Logs):** `docker-compose logs -f`
- **تحديث النظام:** عند إجراء أي تغييرات برمجية، قم بسحب التحديثات (git pull) ثم أعد بناء الحاويات: `docker-compose up -d --build`
